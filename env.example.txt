APP_ENV=development
API_HOST=127.0.0.1
API_PORT=8000

# Default local mode uses a machine-local SQLite file outside the repo so API keys
# and preferences stay on the computer and are not pushed with Git.
# For the target stack, switch this to PostgreSQL + TimescaleDB.
# DATABASE_URL=sqlite:///C:/Users/yourname/AppData/Local/HFT-Bot-2.0/hft_platform.db
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/hft_platform

# Redis is the preferred event bus backend. If unavailable, the app falls back to in-memory pub/sub.
REDIS_URL=redis://localhost:6379/0
EVENT_BUS_BACKEND=auto

LOG_LEVEL=INFO
SIMULATE_MARKET_DATA=true
MAX_CAPITAL_PER_TRADE=5000
MAX_OPEN_POSITIONS=5
MAX_DAILY_LOSS=1000

# Launcher-only options for Git-based auto update.
# GIT_AUTO_UPDATE_ENABLED=true
# GIT_AUTO_UPDATE_INTERVAL_SECONDS=60
# GIT_AUTO_APPLY_ENABLED=false

